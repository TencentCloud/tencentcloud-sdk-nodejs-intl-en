/*
 * Copyright (c) 2018 Tencent. All Rights Reserved.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */
const AbstractModel = require("../../common/abstract_model");

/**
 * DescribeTokenPlanList response structure.
 * @class
 */
class DescribeTokenPlanListResponse extends  AbstractModel {
    constructor(){
        super();

        /**
         * List of package options.
         * @type {Array.<TokenPlanListItem> || null}
         */
        this.TokenPlanSet = null;

        /**
         * Total number of packages.
         * @type {number || null}
         */
        this.TotalCount = null;

        /**
         * The unique request ID, generated by the server, will be returned for every request (if the request fails to reach the server for other reasons, the request will not obtain a RequestId). RequestId is required for locating a problem.
         * @type {string || null}
         */
        this.RequestId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }

        if (params.TokenPlanSet) {
            this.TokenPlanSet = new Array();
            for (let z in params.TokenPlanSet) {
                let obj = new TokenPlanListItem();
                obj.deserialize(params.TokenPlanSet[z]);
                this.TokenPlanSet.push(obj);
            }
        }
        this.TotalCount = 'TotalCount' in params ? params.TotalCount : null;
        this.RequestId = 'RequestId' in params ? params.RequestId : null;

    }
}

/**
 * Termbase detail
 * @class
 */
class GlossaryItem extends  AbstractModel {
    constructor(){
        super();

        /**
         * Termbase ID.
         * @type {string || null}
         */
        this.GlossaryId = null;

        /**
         * Terminology repository name.
         * @type {string || null}
         */
        this.Name = null;

        /**
         * Repository description.
         * @type {string || null}
         */
        this.Description = null;

        /**
         * Source language code.
         * @type {string || null}
         */
        this.Source = null;

        /**
         * Target language code.
         * @type {string || null}
         */
        this.Target = null;

        /**
         * Creation time.
         * @type {string || null}
         */
        this.CreatedTime = null;

        /**
         * Update time.
         * @type {string || null}
         */
        this.UpdatedTime = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.GlossaryId = 'GlossaryId' in params ? params.GlossaryId : null;
        this.Name = 'Name' in params ? params.Name : null;
        this.Description = 'Description' in params ? params.Description : null;
        this.Source = 'Source' in params ? params.Source : null;
        this.Target = 'Target' in params ? params.Target : null;
        this.CreatedTime = 'CreatedTime' in params ? params.CreatedTime : null;
        this.UpdatedTime = 'UpdatedTime' in params ? params.UpdatedTime : null;

    }
}

/**
 * Bind Resource
 * @class
 */
class BindingItem extends  AbstractModel {
    constructor(){
        super();

        /**
         * Resource ID (model ID or service ID).
         * @type {string || null}
         */
        this.ResourceId = null;

        /**
         * Resource type. Value: endpoint (service), model (model).
         * @type {string || null}
         */
        this.ResourceType = null;

        /**
         * Resource status
         * @type {string || null}
         */
        this.Status = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.ResourceId = 'ResourceId' in params ? params.ResourceId : null;
        this.ResourceType = 'ResourceType' in params ? params.ResourceType : null;
        this.Status = 'Status' in params ? params.Status : null;

    }
}

/**
 * DescribeTokenPlanApiKeySecret response structure.
 * @class
 */
class DescribeTokenPlanApiKeySecretResponse extends  AbstractModel {
    constructor(){
        super();

        /**
         * APIKey ID.
         * @type {string || null}
         */
        this.ApiKeyId = null;

        /**
         * APIKey key value (plaintext). Keep it safe.
         * @type {string || null}
         */
        this.ApiKey = null;

        /**
         * The unique request ID, generated by the server, will be returned for every request (if the request fails to reach the server for other reasons, the request will not obtain a RequestId). RequestId is required for locating a problem.
         * @type {string || null}
         */
        this.RequestId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.ApiKeyId = 'ApiKeyId' in params ? params.ApiKeyId : null;
        this.ApiKey = 'ApiKey' in params ? params.ApiKey : null;
        this.RequestId = 'RequestId' in params ? params.RequestId : null;

    }
}

/**
 * Token Plan API Key list item
 * @class
 */
class TokenPlanApiKeyListItem extends  AbstractModel {
    constructor(){
        super();

        /**
         * API Key ID.
         * @type {string || null}
         */
        this.ApiKeyId = null;

        /**
         * API Key secret key value (masking).
         * @type {string || null}
         */
        this.ApiKey = null;

        /**
         * API Key name.
         * @type {string || null}
         */
        this.Name = null;

        /**
         * Bundle ID
         * @type {string || null}
         */
        this.TeamId = null;

        /**
         * Account APP ID.
         * @type {string || null}
         */
        this.AppId = null;

        /**
         * Main account UIN. Maximum 128 characters.
         * @type {string || null}
         */
        this.Uin = null;

        /**
         * API Key available model list (JSON array string).
         * @type {string || null}
         */
        this.AllowedModels = null;

        /**
         * Whether the API Key is available. Values: enable (enable), disable (disable).
         * @type {string || null}
         */
        this.Status = null;

        /**
         * Reason for disabling the API Key. Value: NORMAL (normal, default value), QUOTA_EXHAUSTED (API Key quota package exhausted), ABNORMAL (abnormal, requires human intervention)
         * @type {string || null}
         */
        this.StopReason = null;

        /**
         * User-side switch. Valid values: enable, disable.
         * @type {string || null}
         */
        this.UseStatus = null;

        /**
         * Key version.
         * @type {number || null}
         */
        this.KeyVersion = null;

        /**
         * Last reset time (ISO 8601).
         * @type {string || null}
         */
        this.LastRotatedAt = null;

        /**
         * Creator. If it is created by a sub-account, this value is the sub-account UIN.
         * @type {string || null}
         */
        this.Creator = null;

        /**
         * Creation time.
         * @type {string || null}
         */
        this.CreatedAt = null;

        /**
         * Update time.
         * @type {string || null}
         */
        this.UpdatedAt = null;

        /**
         * API Key limit usage information
         * @type {SubPackageBalance || null}
         */
        this.Balance = null;

        /**
         * Package type. Values: enterprise (Enterprise Professional package), enterprise-auto (Enterprise Light package).
         * @type {string || null}
         */
        this.ProductType = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.ApiKeyId = 'ApiKeyId' in params ? params.ApiKeyId : null;
        this.ApiKey = 'ApiKey' in params ? params.ApiKey : null;
        this.Name = 'Name' in params ? params.Name : null;
        this.TeamId = 'TeamId' in params ? params.TeamId : null;
        this.AppId = 'AppId' in params ? params.AppId : null;
        this.Uin = 'Uin' in params ? params.Uin : null;
        this.AllowedModels = 'AllowedModels' in params ? params.AllowedModels : null;
        this.Status = 'Status' in params ? params.Status : null;
        this.StopReason = 'StopReason' in params ? params.StopReason : null;
        this.UseStatus = 'UseStatus' in params ? params.UseStatus : null;
        this.KeyVersion = 'KeyVersion' in params ? params.KeyVersion : null;
        this.LastRotatedAt = 'LastRotatedAt' in params ? params.LastRotatedAt : null;
        this.Creator = 'Creator' in params ? params.Creator : null;
        this.CreatedAt = 'CreatedAt' in params ? params.CreatedAt : null;
        this.UpdatedAt = 'UpdatedAt' in params ? params.UpdatedAt : null;

        if (params.Balance) {
            let obj = new SubPackageBalance();
            obj.deserialize(params.Balance)
            this.Balance = obj;
        }
        this.ProductType = 'ProductType' in params ? params.ProductType : null;

    }
}

/**
 * Batch create succeeded items
 * @class
 */
class CreateApiKeysResultItem extends  AbstractModel {
    constructor(){
        super();

        /**
         * APIKey ID.
         * @type {string || null}
         */
        this.ApiKeyId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.ApiKeyId = 'ApiKeyId' in params ? params.ApiKeyId : null;

    }
}

/**
 * Create terminology entry
 * @class
 */
class GlossaryEntryInput extends  AbstractModel {
    constructor(){
        super();

        /**
         * Source language terminology. Maximum 1000 characters.
         * @type {string || null}
         */
        this.SourceTerm = null;

        /**
         * Target language terminology. Maximum 1000 characters.
         * @type {string || null}
         */
        this.TargetTerm = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.SourceTerm = 'SourceTerm' in params ? params.SourceTerm : null;
        this.TargetTerm = 'TargetTerm' in params ? params.TargetTerm : null;

    }
}

/**
 * ModifyTokenPlanApiKeySecret response structure.
 * @class
 */
class ModifyTokenPlanApiKeySecretResponse extends  AbstractModel {
    constructor(){
        super();

        /**
         * API Key ID.
         * @type {string || null}
         */
        this.ApiKeyId = null;

        /**
         * Key version after resetting.
         * @type {number || null}
         */
        this.KeyVersion = null;

        /**
         * The unique request ID, generated by the server, will be returned for every request (if the request fails to reach the server for other reasons, the request will not obtain a RequestId). RequestId is required for locating a problem.
         * @type {string || null}
         */
        this.RequestId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.ApiKeyId = 'ApiKeyId' in params ? params.ApiKeyId : null;
        this.KeyVersion = 'KeyVersion' in params ? params.KeyVersion : null;
        this.RequestId = 'RequestId' in params ? params.RequestId : null;

    }
}

/**
 * DescribeTokenPlanApiKeyList response structure.
 * @class
 */
class DescribeTokenPlanApiKeyListResponse extends  AbstractModel {
    constructor(){
        super();

        /**
         * API Key list.
         * @type {Array.<TokenPlanApiKeyListItem> || null}
         */
        this.ApiKeySet = null;

        /**
         * Total number of API Keys.
         * @type {number || null}
         */
        this.TotalCount = null;

        /**
         * The unique request ID, generated by the server, will be returned for every request (if the request fails to reach the server for other reasons, the request will not obtain a RequestId). RequestId is required for locating a problem.
         * @type {string || null}
         */
        this.RequestId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }

        if (params.ApiKeySet) {
            this.ApiKeySet = new Array();
            for (let z in params.ApiKeySet) {
                let obj = new TokenPlanApiKeyListItem();
                obj.deserialize(params.ApiKeySet[z]);
                this.ApiKeySet.push(obj);
            }
        }
        this.TotalCount = 'TotalCount' in params ? params.TotalCount : null;
        this.RequestId = 'RequestId' in params ? params.RequestId : null;

    }
}

/**
 * Delete terminology entry
 * @class
 */
class DeleteGlossaryEntryInput extends  AbstractModel {
    constructor(){
        super();

        /**
         * Terminology entry ID. Obtain through the API DescribeGlossaryEntries.
         * @type {string || null}
         */
        this.EntryId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.EntryId = 'EntryId' in params ? params.EntryId : null;

    }
}

/**
 * DescribeTokenPlan request structure.
 * @class
 */
class DescribeTokenPlanRequest extends  AbstractModel {
    constructor(){
        super();

        /**
         * Package ID. You can obtain it through the DescribeTokenPlanList API.
         * @type {string || null}
         */
        this.TeamId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.TeamId = 'TeamId' in params ? params.TeamId : null;

    }
}

/**
 * Terminology entry detail
 * @class
 */
class GlossaryEntryItem extends  AbstractModel {
    constructor(){
        super();

        /**
         * Terminology entry ID.
         * @type {string || null}
         */
        this.EntryId = null;

        /**
         * Source language terminology.
         * @type {string || null}
         */
        this.SourceTerm = null;

        /**
         * Target language terminology.
         * @type {string || null}
         */
        this.TargetTerm = null;

        /**
         * Last update time. Unix timestamp (ms).
         * @type {number || null}
         */
        this.UpdatedAt = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.EntryId = 'EntryId' in params ? params.EntryId : null;
        this.SourceTerm = 'SourceTerm' in params ? params.SourceTerm : null;
        this.TargetTerm = 'TargetTerm' in params ? params.TargetTerm : null;
        this.UpdatedAt = 'UpdatedAt' in params ? params.UpdatedAt : null;

    }
}

/**
 * DeleteGlossary response structure.
 * @class
 */
class DeleteGlossaryResponse extends  AbstractModel {
    constructor(){
        super();

        /**
         * The unique request ID, generated by the server, will be returned for every request (if the request fails to reach the server for other reasons, the request will not obtain a RequestId). RequestId is required for locating a problem.
         * @type {string || null}
         */
        this.RequestId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.RequestId = 'RequestId' in params ? params.RequestId : null;

    }
}

/**
 * DescribeTokenPlanApiKey response structure.
 * @class
 */
class DescribeTokenPlanApiKeyResponse extends  AbstractModel {
    constructor(){
        super();

        /**
         * API Key details.
         * @type {TokenPlanApiKeyInfo || null}
         */
        this.ApiKey = null;

        /**
         * API Key limit and usage information.
         * @type {SubPackageBalance || null}
         */
        this.Balance = null;

        /**
         * The unique request ID, generated by the server, will be returned for every request (if the request fails to reach the server for other reasons, the request will not obtain a RequestId). RequestId is required for locating a problem.
         * @type {string || null}
         */
        this.RequestId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }

        if (params.ApiKey) {
            let obj = new TokenPlanApiKeyInfo();
            obj.deserialize(params.ApiKey)
            this.ApiKey = obj;
        }

        if (params.Balance) {
            let obj = new SubPackageBalance();
            obj.deserialize(params.Balance)
            this.Balance = obj;
        }
        this.RequestId = 'RequestId' in params ? params.RequestId : null;

    }
}

/**
 * ModifyTokenPlanApiKeySecret request structure.
 * @class
 */
class ModifyTokenPlanApiKeySecretRequest extends  AbstractModel {
    constructor(){
        super();

        /**
         * API Key ID. You can obtain it through the DescribeTokenPlanApiKeyList API.
         * @type {string || null}
         */
        this.ApiKeyId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.ApiKeyId = 'ApiKeyId' in params ? params.ApiKeyId : null;

    }
}

/**
 * API Key limit and usage information
 * @class
 */
class SubPackageBalance extends  AbstractModel {
    constructor(){
        super();

        /**
         * Dedicated limit. Units are as follows:
-Package type: professional. Measurement unit: point.
- Package type is lite package, and the measurement unit is token.
         * @type {string || null}
         */
        this.ExclusiveQuota = null;

        /**
         * Used amount of the dedicated limit. The measurement units are as follows:
-Package type: professional. Measurement unit: point.
- Package type is lite package, and the measurement unit is token.
         * @type {string || null}
         */
        this.ExclusiveUsed = null;

        /**
         * Remaining exclusive quota. Units are as follows:
-Package type: professional. Measurement unit: point.
-Package type: lite package. Measurement unit: token.
         * @type {string || null}
         */
        this.ExclusiveRemain = null;

        /**
         * Shared credit limit. -1 means unlimited. Measurement units are as follows:
-Package type is professional package, measurement unit value is point;
-Package type: lite package. Measurement unit: token.
         * @type {string || null}
         */
        this.SharedQuota = null;

        /**
         * Used amount of the shared quota. Measurement units are as follows:
-Package type is professional package, measurement unit value is point;
-Package type: lite package. Measurement unit: token.
         * @type {string || null}
         */
        this.SharedUsed = null;

        /**
         * Remaining shared quota. Units are described as follows:
-Package type is professional package, measurement unit value is point;
-Package type: lite package. Measurement unit: token.
         * @type {string || null}
         */
        this.SharedRemain = null;

        /**
         * API Key package status. Valid values: 0 (normal), 1 (exhausted).
         * @type {number || null}
         */
        this.Status = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.ExclusiveQuota = 'ExclusiveQuota' in params ? params.ExclusiveQuota : null;
        this.ExclusiveUsed = 'ExclusiveUsed' in params ? params.ExclusiveUsed : null;
        this.ExclusiveRemain = 'ExclusiveRemain' in params ? params.ExclusiveRemain : null;
        this.SharedQuota = 'SharedQuota' in params ? params.SharedQuota : null;
        this.SharedUsed = 'SharedUsed' in params ? params.SharedUsed : null;
        this.SharedRemain = 'SharedRemain' in params ? params.SharedRemain : null;
        this.Status = 'Status' in params ? params.Status : null;

    }
}

/**
 * DeleteGlossaryEntries response structure.
 * @class
 */
class DeleteGlossaryEntriesResponse extends  AbstractModel {
    constructor(){
        super();

        /**
         * The unique request ID, generated by the server, will be returned for every request (if the request fails to reach the server for other reasons, the request will not obtain a RequestId). RequestId is required for locating a problem.
         * @type {string || null}
         */
        this.RequestId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.RequestId = 'RequestId' in params ? params.RequestId : null;

    }
}

/**
 * DescribeApiKeyList response structure.
 * @class
 */
class DescribeApiKeyListResponse extends  AbstractModel {
    constructor(){
        super();

        /**
         * API Key List.
         * @type {Array.<ApiKeyDetail> || null}
         */
        this.ApiKeySet = null;

        /**
         * Total number of eligible API keys.
         * @type {number || null}
         */
        this.TotalCount = null;

        /**
         * The unique request ID, generated by the server, will be returned for every request (if the request fails to reach the server for other reasons, the request will not obtain a RequestId). RequestId is required for locating a problem.
         * @type {string || null}
         */
        this.RequestId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }

        if (params.ApiKeySet) {
            this.ApiKeySet = new Array();
            for (let z in params.ApiKeySet) {
                let obj = new ApiKeyDetail();
                obj.deserialize(params.ApiKeySet[z]);
                this.ApiKeySet.push(obj);
            }
        }
        this.TotalCount = 'TotalCount' in params ? params.TotalCount : null;
        this.RequestId = 'RequestId' in params ? params.RequestId : null;

    }
}

/**
 * RenewTokenPlanTeamOrder response structure.
 * @class
 */
class RenewTokenPlanTeamOrderResponse extends  AbstractModel {
    constructor(){
        super();

        /**
         * Tencent Cloud order ID. Used to associate all sub-orders under a renewal operation.
         * @type {string || null}
         */
        this.BigOrderId = null;

        /**
         * The unique request ID, generated by the server, will be returned for every request (if the request fails to reach the server for other reasons, the request will not obtain a RequestId). RequestId is required for locating a problem.
         * @type {string || null}
         */
        this.RequestId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.BigOrderId = 'BigOrderId' in params ? params.BigOrderId : null;
        this.RequestId = 'RequestId' in params ? params.RequestId : null;

    }
}

/**
 * Token Plan API Key details
 * @class
 */
class TokenPlanApiKeyInfo extends  AbstractModel {
    constructor(){
        super();

        /**
         * API Key ID.
         * @type {string || null}
         */
        this.ApiKeyId = null;

        /**
         * API Key secret key value (masked).
         * @type {string || null}
         */
        this.ApiKey = null;

        /**
         * API Key name.
         * @type {string || null}
         */
        this.Name = null;

        /**
         * Bundle ID.
         * @type {string || null}
         */
        this.TeamId = null;

        /**
         * Account APP ID.
         * @type {string || null}
         */
        this.AppId = null;

        /**
         * Main account UIN.
         * @type {string || null}
         */
        this.Uin = null;

        /**
         * API Key available model list (JSON array string).
         * @type {string || null}
         */
        this.AllowedModels = null;

        /**
         * Whether the API Key is available. Values: enable (enable), disable (disable).
         * @type {string || null}
         */
        this.Status = null;

        /**
         * Reason for disabling the API Key. Valid values: NORMAL (normal, default value), QUOTA_EXHAUSTED (API Key quota package exhausted), ABNORMAL (exception, human intervention required)
         * @type {string || null}
         */
        this.StopReason = null;

        /**
         * User-side switch. Valid values: enable, disable.
         * @type {string || null}
         */
        this.UseStatus = null;

        /**
         * Key version.
         * @type {number || null}
         */
        this.KeyVersion = null;

        /**
         * Last reset time (ISO 8601).
         * @type {string || null}
         */
        this.LastRotatedAt = null;

        /**
         * Creator. If it is created by a sub-account, this value is the sub-account UIN.
         * @type {string || null}
         */
        this.Creator = null;

        /**
         * Creation time.
         * @type {string || null}
         */
        this.CreatedAt = null;

        /**
         * Update time.
         * @type {string || null}
         */
        this.UpdatedAt = null;

        /**
         * TPM limit (Tokens Per Minute).
         * @type {number || null}
         */
        this.TPM = null;

        /**
         * Package type. Values: enterprise (Enterprise Professional package), enterprise-auto (Enterprise Light package)
         * @type {string || null}
         */
        this.ProductType = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.ApiKeyId = 'ApiKeyId' in params ? params.ApiKeyId : null;
        this.ApiKey = 'ApiKey' in params ? params.ApiKey : null;
        this.Name = 'Name' in params ? params.Name : null;
        this.TeamId = 'TeamId' in params ? params.TeamId : null;
        this.AppId = 'AppId' in params ? params.AppId : null;
        this.Uin = 'Uin' in params ? params.Uin : null;
        this.AllowedModels = 'AllowedModels' in params ? params.AllowedModels : null;
        this.Status = 'Status' in params ? params.Status : null;
        this.StopReason = 'StopReason' in params ? params.StopReason : null;
        this.UseStatus = 'UseStatus' in params ? params.UseStatus : null;
        this.KeyVersion = 'KeyVersion' in params ? params.KeyVersion : null;
        this.LastRotatedAt = 'LastRotatedAt' in params ? params.LastRotatedAt : null;
        this.Creator = 'Creator' in params ? params.Creator : null;
        this.CreatedAt = 'CreatedAt' in params ? params.CreatedAt : null;
        this.UpdatedAt = 'UpdatedAt' in params ? params.UpdatedAt : null;
        this.TPM = 'TPM' in params ? params.TPM : null;
        this.ProductType = 'ProductType' in params ? params.ProductType : null;

    }
}

/**
 * ModifyApiKeyInfo response structure.
 * @class
 */
class ModifyApiKeyInfoResponse extends  AbstractModel {
    constructor(){
        super();

        /**
         * The unique request ID, generated by the server, will be returned for every request (if the request fails to reach the server for other reasons, the request will not obtain a RequestId). RequestId is required for locating a problem.
         * @type {string || null}
         */
        this.RequestId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.RequestId = 'RequestId' in params ? params.RequestId : null;

    }
}

/**
 * ModifyTokenPlanApiKey response structure.
 * @class
 */
class ModifyTokenPlanApiKeyResponse extends  AbstractModel {
    constructor(){
        super();

        /**
         * The unique request ID, generated by the server, will be returned for every request (if the request fails to reach the server for other reasons, the request will not obtain a RequestId). RequestId is required for locating a problem.
         * @type {string || null}
         */
        this.RequestId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.RequestId = 'RequestId' in params ? params.RequestId : null;

    }
}

/**
 * DescribeApiKeyList request structure.
 * @class
 */
class DescribeApiKeyListRequest extends  AbstractModel {
    constructor(){
        super();

        /**
         * Platform type. Currently supported values: maas.
         * @type {string || null}
         */
        this.Platform = null;

        /**
         * Number of returned results, defaults to 20, maximum value 100.
         * @type {number || null}
         */
        this.Limit = null;

        /**
         * Offset. Default value: 0.
         * @type {number || null}
         */
        this.Offset = null;

        /**
         * Filter condition list. Supported filter fields: apikeyId (API Key ID), apiKeyName (name), platform (platform type), status (status), bindType (binding type).
         * @type {Array.<RequestFilter> || null}
         */
        this.Filters = null;

        /**
         * Sorting condition list. Supported sorting field: apiKeyName
         * @type {Array.<RequestSort> || null}
         */
        this.Sorts = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.Platform = 'Platform' in params ? params.Platform : null;
        this.Limit = 'Limit' in params ? params.Limit : null;
        this.Offset = 'Offset' in params ? params.Offset : null;

        if (params.Filters) {
            this.Filters = new Array();
            for (let z in params.Filters) {
                let obj = new RequestFilter();
                obj.deserialize(params.Filters[z]);
                this.Filters.push(obj);
            }
        }

        if (params.Sorts) {
            this.Sorts = new Array();
            for (let z in params.Sorts) {
                let obj = new RequestSort();
                obj.deserialize(params.Sorts[z]);
                this.Sorts.push(obj);
            }
        }

    }
}

/**
 * CreateGlossary request structure.
 * @class
 */
class CreateGlossaryRequest extends  AbstractModel {
    constructor(){
        super();

        /**
         * Terminology library name. Maximum 50 characters.
         * @type {string || null}
         */
        this.Name = null;

        /**
         * Source language code. Maximum 16 characters, such as zh (Chinese), en (English).
         * @type {string || null}
         */
        this.Source = null;

        /**
         * Target language code. Maximum 16 characters, such as zh (Chinese), en (English).
         * @type {string || null}
         */
        this.Target = null;

        /**
         * Termbase description. Maximum 255 characters.
         * @type {string || null}
         */
        this.Description = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.Name = 'Name' in params ? params.Name : null;
        this.Source = 'Source' in params ? params.Source : null;
        this.Target = 'Target' in params ? params.Target : null;
        this.Description = 'Description' in params ? params.Description : null;

    }
}

/**
 * DescribeGlossaryEntries response structure.
 * @class
 */
class DescribeGlossaryEntriesResponse extends  AbstractModel {
    constructor(){
        super();

        /**
         * Terminology entry list.
         * @type {Array.<GlossaryEntryItem> || null}
         */
        this.Entries = null;

        /**
         * Total number of eligible terminology entries.
         * @type {number || null}
         */
        this.Total = null;

        /**
         * Current page.
         * @type {number || null}
         */
        this.Page = null;

        /**
         * Size per page
         * @type {number || null}
         */
        this.PageSize = null;

        /**
         * The unique request ID, generated by the server, will be returned for every request (if the request fails to reach the server for other reasons, the request will not obtain a RequestId). RequestId is required for locating a problem.
         * @type {string || null}
         */
        this.RequestId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }

        if (params.Entries) {
            this.Entries = new Array();
            for (let z in params.Entries) {
                let obj = new GlossaryEntryItem();
                obj.deserialize(params.Entries[z]);
                this.Entries.push(obj);
            }
        }
        this.Total = 'Total' in params ? params.Total : null;
        this.Page = 'Page' in params ? params.Page : null;
        this.PageSize = 'PageSize' in params ? params.PageSize : null;
        this.RequestId = 'RequestId' in params ? params.RequestId : null;

    }
}

/**
 * DeleteGlossary request structure.
 * @class
 */
class DeleteGlossaryRequest extends  AbstractModel {
    constructor(){
        super();

        /**
         * Termbase ID. Obtain through the API DescribeGlossaries.
         * @type {string || null}
         */
        this.GlossaryId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.GlossaryId = 'GlossaryId' in params ? params.GlossaryId : null;

    }
}

/**
 * DescribeGlossaryEntries request structure.
 * @class
 */
class DescribeGlossaryEntriesRequest extends  AbstractModel {
    constructor(){
        super();

        /**
         * Termbase ID. Obtain through the API DescribeGlossaries.
         * @type {string || null}
         */
        this.GlossaryId = null;

        /**
         * Page number. Default is 1.
         * @type {number || null}
         */
        this.Page = null;

        /**
         * Size per page. The default value is 20, and the maximum value is 200.
         * @type {number || null}
         */
        this.PageSize = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.GlossaryId = 'GlossaryId' in params ? params.GlossaryId : null;
        this.Page = 'Page' in params ? params.Page : null;
        this.PageSize = 'PageSize' in params ? params.PageSize : null;

    }
}

/**
 * DeleteGlossaryEntries request structure.
 * @class
 */
class DeleteGlossaryEntriesRequest extends  AbstractModel {
    constructor(){
        super();

        /**
         * Termbase ID. Obtain through the API DescribeGlossaries.
         * @type {string || null}
         */
        this.GlossaryId = null;

        /**
         * Terminology entry list to be deleted. At a time 200.
         * @type {Array.<DeleteGlossaryEntryInput> || null}
         */
        this.Entries = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.GlossaryId = 'GlossaryId' in params ? params.GlossaryId : null;

        if (params.Entries) {
            this.Entries = new Array();
            for (let z in params.Entries) {
                let obj = new DeleteGlossaryEntryInput();
                obj.deserialize(params.Entries[z]);
                this.Entries.push(obj);
            }
        }

    }
}

/**
 * CreateGlossaryEntries response structure.
 * @class
 */
class CreateGlossaryEntriesResponse extends  AbstractModel {
    constructor(){
        super();

        /**
         * List of successfully created terminology entries.
         * @type {Array.<GlossaryEntryItem> || null}
         */
        this.Entries = null;

        /**
         * The unique request ID, generated by the server, will be returned for every request (if the request fails to reach the server for other reasons, the request will not obtain a RequestId). RequestId is required for locating a problem.
         * @type {string || null}
         */
        this.RequestId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }

        if (params.Entries) {
            this.Entries = new Array();
            for (let z in params.Entries) {
                let obj = new GlossaryEntryItem();
                obj.deserialize(params.Entries[z]);
                this.Entries.push(obj);
            }
        }
        this.RequestId = 'RequestId' in params ? params.RequestId : null;

    }
}

/**
 * CreateTokenPlanApiKeys response structure.
 * @class
 */
class CreateTokenPlanApiKeysResponse extends  AbstractModel {
    constructor(){
        super();

        /**
         * Item list of successful creation.
         * @type {Array.<CreateApiKeysResultItem> || null}
         */
        this.Items = null;

        /**
         * Item list that failed to be created.
         * @type {Array.<BatchCreateFailedItem> || null}
         */
        this.FailedItems = null;

        /**
         * The unique request ID, generated by the server, will be returned for every request (if the request fails to reach the server for other reasons, the request will not obtain a RequestId). RequestId is required for locating a problem.
         * @type {string || null}
         */
        this.RequestId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }

        if (params.Items) {
            this.Items = new Array();
            for (let z in params.Items) {
                let obj = new CreateApiKeysResultItem();
                obj.deserialize(params.Items[z]);
                this.Items.push(obj);
            }
        }

        if (params.FailedItems) {
            this.FailedItems = new Array();
            for (let z in params.FailedItems) {
                let obj = new BatchCreateFailedItem();
                obj.deserialize(params.FailedItems[z]);
                this.FailedItems.push(obj);
            }
        }
        this.RequestId = 'RequestId' in params ? params.RequestId : null;

    }
}

/**
 * DescribeTokenPlanApiKey request structure.
 * @class
 */
class DescribeTokenPlanApiKeyRequest extends  AbstractModel {
    constructor(){
        super();

        /**
         * API Key ID. You can obtain it through the DescribeTokenPlanApiKeyList API.
         * @type {string || null}
         */
        this.ApiKeyId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.ApiKeyId = 'ApiKeyId' in params ? params.ApiKeyId : null;

    }
}

/**
 * DescribeApiKey request structure.
 * @class
 */
class DescribeApiKeyRequest extends  AbstractModel {
    constructor(){
        super();

        /**
         * Platform type. Currently supported values: maas.
         * @type {string || null}
         */
        this.Platform = null;

        /**
         * API Key ID. At least one of this or ApiKey is required. Prioritize ApiKeyId.
         * @type {string || null}
         */
        this.ApiKeyId = null;

        /**
         * API key plaintext. At least one of it and ApiKeyId must be imported.
         * @type {string || null}
         */
        this.ApiKey = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.Platform = 'Platform' in params ? params.Platform : null;
        this.ApiKeyId = 'ApiKeyId' in params ? params.ApiKeyId : null;
        this.ApiKey = 'ApiKey' in params ? params.ApiKey : null;

    }
}

/**
 * DeleteApiKey response structure.
 * @class
 */
class DeleteApiKeyResponse extends  AbstractModel {
    constructor(){
        super();

        /**
         * The unique request ID, generated by the server, will be returned for every request (if the request fails to reach the server for other reasons, the request will not obtain a RequestId). RequestId is required for locating a problem.
         * @type {string || null}
         */
        this.RequestId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.RequestId = 'RequestId' in params ? params.RequestId : null;

    }
}

/**
 * Modify terminology entry
 * @class
 */
class ModifyGlossaryEntryInput extends  AbstractModel {
    constructor(){
        super();

        /**
         * Terminology entry ID. Obtain through the API DescribeGlossaryEntries.
         * @type {string || null}
         */
        this.EntryId = null;

        /**
         * Source language terminology. Maximum 1000 characters. If not passed, remain unchanged.
         * @type {string || null}
         */
        this.SourceTerm = null;

        /**
         * Target language terminology. Maximum 1000 characters. Remain unchanged if not passed.
         * @type {string || null}
         */
        this.TargetTerm = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.EntryId = 'EntryId' in params ? params.EntryId : null;
        this.SourceTerm = 'SourceTerm' in params ? params.SourceTerm : null;
        this.TargetTerm = 'TargetTerm' in params ? params.TargetTerm : null;

    }
}

/**
 * ModifyTokenPlanApiKey request structure.
 * @class
 */
class ModifyTokenPlanApiKeyRequest extends  AbstractModel {
    constructor(){
        super();

        /**
         * API Key ID.
         * @type {string || null}
         */
        this.ApiKeyId = null;

        /**
         * Available model list. If this parameter is not specified, no modification is made.

- If the package type is enterprise professional:
1) Input "all": use all models supported by the package
2) Import Model ID: specify a specific model. "all" and a specific Model ID cannot be specified at the same time.

-If the package type is enterprise lightweight edition, do not pass in this parameter.
         * @type {Array.<string> || null}
         */
        this.AllowedModels = null;

        /**
         * Dedicated limit. If this parameter is not specified, no modification will be made. Unit:

-Package type: professional. Measurement unit: point.
- Package type is lite package, and the measurement unit is token.
         * @type {number || null}
         */
        this.ExclusiveQuota = null;

        /**
         * Total credit limit. -1 means unlimited. It must be -1 or greater than or equal to the current ExclusiveQuota of the API Key. If not passed, no modification is made. Measurement units are as follows:
-Package type: professional. Measurement unit: point.
- Package type is lite package, and the measurement unit is token.
         * @type {number || null}
         */
        this.TotalQuota = null;

        /**
         * Whether to enable the API Key. Values: enable (enable), disable (disable). If not passed, no modification is made.
         * @type {string || null}
         */
        this.UseStatus = null;

        /**
         * TPM (Tokens Per Minute) limit. If not passed, no modification will be made. Must be >= 0 and <= the package TPM.
         * @type {number || null}
         */
        this.TPM = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.ApiKeyId = 'ApiKeyId' in params ? params.ApiKeyId : null;
        this.AllowedModels = 'AllowedModels' in params ? params.AllowedModels : null;
        this.ExclusiveQuota = 'ExclusiveQuota' in params ? params.ExclusiveQuota : null;
        this.TotalQuota = 'TotalQuota' in params ? params.TotalQuota : null;
        this.UseStatus = 'UseStatus' in params ? params.UseStatus : null;
        this.TPM = 'TPM' in params ? params.TPM : null;

    }
}

/**
 * Billing item for Token aggregation
 * @class
 */
class TokenSummaryBillingItem extends  AbstractModel {
    constructor(){
        super();

        /**
         * Billing item. Values: input (input Token), output (output Token), cache (cache Token), call_count (call count).
         * @type {string || null}
         */
        this.BillingItem = null;

        /**
         * Aggregated raw usage of this billing item during a period. Unit: tokens.
         * @type {number || null}
         */
        this.TotalQty = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.BillingItem = 'BillingItem' in params ? params.BillingItem : null;
        this.TotalQty = 'TotalQty' in params ? params.TotalQty : null;

    }
}

/**
 * DescribeTokenPlanApiKeyUsageDetail response structure.
 * @class
 */
class DescribeTokenPlanApiKeyUsageDetailResponse extends  AbstractModel {
    constructor(){
        super();

        /**
         * The unique request ID, generated by the server, will be returned for every request (if the request fails to reach the server for other reasons, the request will not obtain a RequestId). RequestId is required for locating a problem.
         * @type {string || null}
         */
        this.RequestId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.RequestId = 'RequestId' in params ? params.RequestId : null;

    }
}

/**
 * Sort criteria
 * @class
 */
class RequestSort extends  AbstractModel {
    constructor(){
        super();

        /**
         * Sorting field name.
         * @type {string || null}
         */
        this.Name = null;

        /**
         * Sorting order. Value: ASC (ascending), DESC (descending).
         * @type {string || null}
         */
        this.Order = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.Name = 'Name' in params ? params.Name : null;
        this.Order = 'Order' in params ? params.Order : null;

    }
}

/**
 * ModifyApiKeyInfo request structure.
 * @class
 */
class ModifyApiKeyInfoRequest extends  AbstractModel {
    constructor(){
        super();

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }

    }
}

/**
 * Token Plan package option
 * @class
 */
class TokenPlanListItem extends  AbstractModel {
    constructor(){
        super();

        /**
         * <p>Package ID.</p>
         * @type {string || null}
         */
        this.TeamId = null;

        /**
         * <p>Package type. Values: enterprise (Enterprise Professional package), enterprise-auto (Enterprise Light package)</p>
         * @type {string || null}
         */
        this.ProductType = null;

        /**
         * <p>Package name. Maximum 128 characters.</p>
         * @type {string || null}
         */
        this.Name = null;

        /**
         * <p>Account APP ID.</p>
         * @type {string || null}
         */
        this.AppId = null;

        /**
         * <p>Main account UIN.</p>
         * @type {string || null}
         */
        this.Uin = null;

        /**
         * <p>Package status. Valid values: enable, disable.</p>
         * @type {string || null}
         */
        this.Status = null;

        /**
         * <p>Reason for package disablement. Value: NORMAL, ISOLATED, FROZEN, EXHAUSTED, DESTROYED</p>
         * @type {string || null}
         */
        this.StopReason = null;

        /**
         * <p>Maximum number of API Keys that can be created.</p>
         * @type {number || null}
         */
        this.ApiKeyMax = null;

        /**
         * <p>Number of API Keys currently created</p>
         * @type {number || null}
         */
        this.ApiKeyCount = null;

        /**
         * <p>Cloud billing prepaid resource package ID.</p>
         * @type {string || null}
         */
        this.PrepayResourceID = null;

        /**
         * <p>Creator. If the package is created by a sub-account, this value is the sub-account UIN.</p>
         * @type {string || null}
         */
        this.Creator = null;

        /**
         * <p>Creation time.</p>
         * @type {string || null}
         */
        this.CreatedAt = null;

        /**
         * <p>Update time.</p>
         * @type {string || null}
         */
        this.UpdatedAt = null;

        /**
         * <p>Basic information of the package.</p>
         * @type {TokenPlanPackageInfo || null}
         */
        this.PackageInfo = null;

        /**
         * <p>Whether to enable auto-renewal. Value: 0 (not enabled), 1 (enabled)</p>
         * @type {number || null}
         */
        this.AutoRenewFlag = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.TeamId = 'TeamId' in params ? params.TeamId : null;
        this.ProductType = 'ProductType' in params ? params.ProductType : null;
        this.Name = 'Name' in params ? params.Name : null;
        this.AppId = 'AppId' in params ? params.AppId : null;
        this.Uin = 'Uin' in params ? params.Uin : null;
        this.Status = 'Status' in params ? params.Status : null;
        this.StopReason = 'StopReason' in params ? params.StopReason : null;
        this.ApiKeyMax = 'ApiKeyMax' in params ? params.ApiKeyMax : null;
        this.ApiKeyCount = 'ApiKeyCount' in params ? params.ApiKeyCount : null;
        this.PrepayResourceID = 'PrepayResourceID' in params ? params.PrepayResourceID : null;
        this.Creator = 'Creator' in params ? params.Creator : null;
        this.CreatedAt = 'CreatedAt' in params ? params.CreatedAt : null;
        this.UpdatedAt = 'UpdatedAt' in params ? params.UpdatedAt : null;

        if (params.PackageInfo) {
            let obj = new TokenPlanPackageInfo();
            obj.deserialize(params.PackageInfo)
            this.PackageInfo = obj;
        }
        this.AutoRenewFlag = 'AutoRenewFlag' in params ? params.AutoRenewFlag : null;

    }
}

/**
 * CreateGlossary response structure.
 * @class
 */
class CreateGlossaryResponse extends  AbstractModel {
    constructor(){
        super();

        /**
         * Termbase ID.
         * @type {string || null}
         */
        this.GlossaryId = null;

        /**
         * Terminology repository name.
         * @type {string || null}
         */
        this.Name = null;

        /**
         * Creation time. Unix timestamp (ms).
         * @type {number || null}
         */
        this.CreatedAt = null;

        /**
         * The unique request ID, generated by the server, will be returned for every request (if the request fails to reach the server for other reasons, the request will not obtain a RequestId). RequestId is required for locating a problem.
         * @type {string || null}
         */
        this.RequestId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.GlossaryId = 'GlossaryId' in params ? params.GlossaryId : null;
        this.Name = 'Name' in params ? params.Name : null;
        this.CreatedAt = 'CreatedAt' in params ? params.CreatedAt : null;
        this.RequestId = 'RequestId' in params ? params.RequestId : null;

    }
}

/**
 * CreateTokenPlanTeamOrderAndBuy request structure.
 * @class
 */
class CreateTokenPlanTeamOrderAndBuyRequest extends  AbstractModel {
    constructor(){
        super();

        /**
         * <p>Package type. Value: enterprise (enterprise edition professional package), enterprise-auto (enterprise edition lite package).</p>
         * @type {string || null}
         */
        this.ProductType = null;

        /**
         * <p>Package name. It can only contain Chinese, letters, digits, underscores, and hyphens. It must start with a Chinese character or a letter and end with a Chinese character, letter, or digit. The length should be 2-50 characters.</p>
         * @type {string || null}
         */
        this.TeamName = null;

        /**
         * <p>Purchase duration. Unit: Month. It must be greater than 0, supporting 1 to 12 months.</p>
         * @type {number || null}
         */
        this.TimeSpan = null;

        /**
         * <p>Specification of the purchased package. If the package type is enterprise, the measurement unit is point; if the package type is enterprise-auto, the measurement unit is tokens.</p>
         * @type {number || null}
         */
        this.CreditOrToken = null;

        /**
         * <p>Whether to enable auto-renewal. Not enabled by default.</p>
         * @type {boolean || null}
         */
        this.EnableAutoRenew = null;

        /**
         * <p>Existing package ID (if not empty, the renewal process is performed; if empty, a new purchase is performed)</p>
         * @type {string || null}
         */
        this.TeamId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.ProductType = 'ProductType' in params ? params.ProductType : null;
        this.TeamName = 'TeamName' in params ? params.TeamName : null;
        this.TimeSpan = 'TimeSpan' in params ? params.TimeSpan : null;
        this.CreditOrToken = 'CreditOrToken' in params ? params.CreditOrToken : null;
        this.EnableAutoRenew = 'EnableAutoRenew' in params ? params.EnableAutoRenew : null;
        this.TeamId = 'TeamId' in params ? params.TeamId : null;

    }
}

/**
 * ModifyGlossaryEntries request structure.
 * @class
 */
class ModifyGlossaryEntriesRequest extends  AbstractModel {
    constructor(){
        super();

        /**
         * Termbase ID. Obtain through the API DescribeGlossaries.
         * @type {string || null}
         */
        this.GlossaryId = null;

        /**
         * Terminology entry list. At a time 200.
         * @type {Array.<ModifyGlossaryEntryInput> || null}
         */
        this.Entries = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.GlossaryId = 'GlossaryId' in params ? params.GlossaryId : null;

        if (params.Entries) {
            this.Entries = new Array();
            for (let z in params.Entries) {
                let obj = new ModifyGlossaryEntryInput();
                obj.deserialize(params.Entries[z]);
                this.Entries.push(obj);
            }
        }

    }
}

/**
 * RenewTokenPlanTeamOrder request structure.
 * @class
 */
class RenewTokenPlanTeamOrderRequest extends  AbstractModel {
    constructor(){
        super();

        /**
         * Package ID, which can be obtained through the DescribeTokenPlanList API.
         * @type {string || null}
         */
        this.TeamId = null;

        /**
         * Renewal duration. Unit: month. Must be greater than 0.
         * @type {number || null}
         */
        this.TimeSpan = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.TeamId = 'TeamId' in params ? params.TeamId : null;
        this.TimeSpan = 'TimeSpan' in params ? params.TimeSpan : null;

    }
}

/**
 * DescribeGlossaries request structure.
 * @class
 */
class DescribeGlossariesRequest extends  AbstractModel {
    constructor(){
        super();

        /**
         * Number of returned results. Defaults to 20, maximum value is 100.
         * @type {number || null}
         */
        this.Limit = null;

        /**
         * Offset. Default value: 0.
         * @type {number || null}
         */
        this.Offset = null;

        /**
         * Filter condition list. Supported filter fields: GlossaryId (Termbase ID), Name, Source (source language code), Target (target language code).
         * @type {Array.<RequestFilter> || null}
         */
        this.Filters = null;

        /**
         * Sorting condition list. Supported sorting fields: CreatedTime (creation time), UpdatedTime (last update time).
         * @type {Array.<RequestSort> || null}
         */
        this.Sorts = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.Limit = 'Limit' in params ? params.Limit : null;
        this.Offset = 'Offset' in params ? params.Offset : null;

        if (params.Filters) {
            this.Filters = new Array();
            for (let z in params.Filters) {
                let obj = new RequestFilter();
                obj.deserialize(params.Filters[z]);
                this.Filters.push(obj);
            }
        }

        if (params.Sorts) {
            this.Sorts = new Array();
            for (let z in params.Sorts) {
                let obj = new RequestSort();
                obj.deserialize(params.Sorts[z]);
                this.Sorts.push(obj);
            }
        }

    }
}

/**
 * DeleteTokenPlanApiKey response structure.
 * @class
 */
class DeleteTokenPlanApiKeyResponse extends  AbstractModel {
    constructor(){
        super();

        /**
         * The unique request ID, generated by the server, will be returned for every request (if the request fails to reach the server for other reasons, the request will not obtain a RequestId). RequestId is required for locating a problem.
         * @type {string || null}
         */
        this.RequestId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.RequestId = 'RequestId' in params ? params.RequestId : null;

    }
}

/**
 * DescribeTokenPlanApiKeyUsageDetail request structure.
 * @class
 */
class DescribeTokenPlanApiKeyUsageDetailRequest extends  AbstractModel {
    constructor(){
        super();

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }

    }
}

/**
 * UpgradeTokenPlanTeamOrder request structure.
 * @class
 */
class UpgradeTokenPlanTeamOrderRequest extends  AbstractModel {
    constructor(){
        super();

        /**
         * Package ID. You can obtain it through the DescribeTokenPlanList API.
         * @type {string || null}
         */
        this.TeamId = null;

        /**
         * Limit of the new specification after upgrade. For the enterprise package type, it refers to the point limit. For the enterprise-auto package type, it refers to the Token count. Must be greater than the current limit.
         * @type {number || null}
         */
        this.NewCreditOrToken = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.TeamId = 'TeamId' in params ? params.TeamId : null;
        this.NewCreditOrToken = 'NewCreditOrToken' in params ? params.NewCreditOrToken : null;

    }
}

/**
 * ModifyApiKeyStatus request structure.
 * @class
 */
class ModifyApiKeyStatusRequest extends  AbstractModel {
    constructor(){
        super();

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }

    }
}

/**
 * Token quota information
 * @class
 */
class QuotaInfo extends  AbstractModel {
    constructor(){
        super();

        /**
         * Quota package ID.
         * @type {string || null}
         */
        this.PkgId = null;

        /**
         * Package status. Value: 1 (normal), 3 (exhausted), 4 (terminated).
         * @type {number || null}
         */
        this.Status = null;

        /**
         * Quota period. Value: d (by day), m (monthly), lifetime (total quota, no reset).
         * @type {string || null}
         */
        this.CycleUnit = null;

        /**
         * Dimensional quota total amount (number of tokens). Use string to avoid precision loss.
         * @type {string || null}
         */
        this.CycleCredits = null;

        /**
         * Dimensional used amount (number of tokens). Use string literal to avoid precision loss.
         * @type {string || null}
         */
        this.CycleUsed = null;

        /**
         * Quota effective start time.
         * @type {string || null}
         */
        this.StartTime = null;

        /**
         * Quota expiration time.
         * @type {string || null}
         */
        this.ExpireTime = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.PkgId = 'PkgId' in params ? params.PkgId : null;
        this.Status = 'Status' in params ? params.Status : null;
        this.CycleUnit = 'CycleUnit' in params ? params.CycleUnit : null;
        this.CycleCredits = 'CycleCredits' in params ? params.CycleCredits : null;
        this.CycleUsed = 'CycleUsed' in params ? params.CycleUsed : null;
        this.StartTime = 'StartTime' in params ? params.StartTime : null;
        this.ExpireTime = 'ExpireTime' in params ? params.ExpireTime : null;

    }
}

/**
 * CreateGlossaryEntries request structure.
 * @class
 */
class CreateGlossaryEntriesRequest extends  AbstractModel {
    constructor(){
        super();

        /**
         * Termbase ID. Obtain through the API DescribeGlossaries.
         * @type {string || null}
         */
        this.GlossaryId = null;

        /**
         * Terminology entry list. At a time 100.
         * @type {Array.<GlossaryEntryInput> || null}
         */
        this.Entries = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.GlossaryId = 'GlossaryId' in params ? params.GlossaryId : null;

        if (params.Entries) {
            this.Entries = new Array();
            for (let z in params.Entries) {
                let obj = new GlossaryEntryInput();
                obj.deserialize(params.Entries[z]);
                this.Entries.push(obj);
            }
        }

    }
}

/**
 * Details of the specified API key
 * @class
 */
class ApiKeyDetail extends  AbstractModel {
    constructor(){
        super();

        /**
         * API Key ID.
         * @type {string || null}
         */
        this.ApiKeyId = null;

        /**
         * Name
         * @type {string || null}
         */
        this.Name = null;

        /**
         * API key value. The API response contains the masking value.
         * @type {string || null}
         */
        this.ApiKey = null;

        /**
         * Remarks.
         * @type {string || null}
         */
        this.Remark = null;

        /**
         * Platform type. Currently supported values: maas.
         * @type {string || null}
         */
        this.Platform = null;

        /**
         * Root account.
         * @type {string || null}
         */
        this.Uin = null;

        /**
         * Sub-account.
         * @type {string || null}
         */
        this.SubUin = null;

        /**
         * Status. Valid values: enable, disable.
         * @type {string || null}
         */
        this.Status = null;

        /**
         * Binding type. Value: all (all models and services), model_all_endpoint_custom (all models + custom service), model_custom_endpoint_all (custom model + all services), model_custom_endpoint_custom (custom model + custom service).
         * @type {string || null}
         */
        this.BindType = null;

        /**
         * Creation time. Format: YYYY-MM-DD HH:mm:ss.
         * @type {string || null}
         */
        this.CreateTime = null;

        /**
         * Last update time. Format: YYYY-MM-DD HH:mm:ss.
         * @type {string || null}
         */
        this.UpdateTime = null;

        /**
         * App ID.
         * @type {string || null}
         */
        this.AppId = null;

        /**
         * Whether it is editable. true means editable, false means non-editable.
         * @type {boolean || null}
         */
        this.Editable = null;

        /**
         * List of bound resources, case-sensitive for endpoint and model kind.
         * @type {Array.<BindingItem> || null}
         */
        this.BindingItems = null;

        /**
         * IP allowlist. Supports IPv4 and CIDR format. Empty array indicates no restriction.
         * @type {Array.<string> || null}
         */
        this.IpWhitelist = null;

        /**
         * This field is empty when Platform is maas.
         * @type {string || null}
         */
        this.Creator = null;

        /**
         * Multi-dimensional list of Token quota information. This field is not returned when unconfigured.
         * @type {Array.<QuotaInfo> || null}
         */
        this.QuotaSet = null;

        /**
         * Token quota status. An empty string means no configuration. active means configured with current availability. inactive means configured but quota exhausted.
         * @type {string || null}
         */
        this.QuotaStatus = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.ApiKeyId = 'ApiKeyId' in params ? params.ApiKeyId : null;
        this.Name = 'Name' in params ? params.Name : null;
        this.ApiKey = 'ApiKey' in params ? params.ApiKey : null;
        this.Remark = 'Remark' in params ? params.Remark : null;
        this.Platform = 'Platform' in params ? params.Platform : null;
        this.Uin = 'Uin' in params ? params.Uin : null;
        this.SubUin = 'SubUin' in params ? params.SubUin : null;
        this.Status = 'Status' in params ? params.Status : null;
        this.BindType = 'BindType' in params ? params.BindType : null;
        this.CreateTime = 'CreateTime' in params ? params.CreateTime : null;
        this.UpdateTime = 'UpdateTime' in params ? params.UpdateTime : null;
        this.AppId = 'AppId' in params ? params.AppId : null;
        this.Editable = 'Editable' in params ? params.Editable : null;

        if (params.BindingItems) {
            this.BindingItems = new Array();
            for (let z in params.BindingItems) {
                let obj = new BindingItem();
                obj.deserialize(params.BindingItems[z]);
                this.BindingItems.push(obj);
            }
        }
        this.IpWhitelist = 'IpWhitelist' in params ? params.IpWhitelist : null;
        this.Creator = 'Creator' in params ? params.Creator : null;

        if (params.QuotaSet) {
            this.QuotaSet = new Array();
            for (let z in params.QuotaSet) {
                let obj = new QuotaInfo();
                obj.deserialize(params.QuotaSet[z]);
                this.QuotaSet.push(obj);
            }
        }
        this.QuotaStatus = 'QuotaStatus' in params ? params.QuotaStatus : null;

    }
}

/**
 * DescribeGlossaries response structure.
 * @class
 */
class DescribeGlossariesResponse extends  AbstractModel {
    constructor(){
        super();

        /**
         * Terminology repository list.
         * @type {Array.<GlossaryItem> || null}
         */
        this.Items = null;

        /**
         * Total number of eligible terminology repositories.
         * @type {number || null}
         */
        this.TotalCount = null;

        /**
         * Current page.
         * @type {number || null}
         */
        this.Current = null;

        /**
         * Size per page
         * @type {number || null}
         */
        this.PageSize = null;

        /**
         * The unique request ID, generated by the server, will be returned for every request (if the request fails to reach the server for other reasons, the request will not obtain a RequestId). RequestId is required for locating a problem.
         * @type {string || null}
         */
        this.RequestId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }

        if (params.Items) {
            this.Items = new Array();
            for (let z in params.Items) {
                let obj = new GlossaryItem();
                obj.deserialize(params.Items[z]);
                this.Items.push(obj);
            }
        }
        this.TotalCount = 'TotalCount' in params ? params.TotalCount : null;
        this.Current = 'Current' in params ? params.Current : null;
        this.PageSize = 'PageSize' in params ? params.PageSize : null;
        this.RequestId = 'RequestId' in params ? params.RequestId : null;

    }
}

/**
 * ModifyApiKeyStatus response structure.
 * @class
 */
class ModifyApiKeyStatusResponse extends  AbstractModel {
    constructor(){
        super();

        /**
         * The unique request ID, generated by the server, will be returned for every request (if the request fails to reach the server for other reasons, the request will not obtain a RequestId). RequestId is required for locating a problem.
         * @type {string || null}
         */
        this.RequestId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.RequestId = 'RequestId' in params ? params.RequestId : null;

    }
}

/**
 * Batch create failed items
 * @class
 */
class BatchCreateFailedItem extends  AbstractModel {
    constructor(){
        super();

        /**
         * Serial number of the failed item (starting from 1, corresponding to the suffix number).
         * @type {number || null}
         */
        this.Index = null;

        /**
         * Name of the failed item.
         * @type {string || null}
         */
        this.Name = null;

        /**
         * Failure reason.
         * @type {string || null}
         */
        this.Reason = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.Index = 'Index' in params ? params.Index : null;
        this.Name = 'Name' in params ? params.Name : null;
        this.Reason = 'Reason' in params ? params.Reason : null;

    }
}

/**
 * CreateApiKey response structure.
 * @class
 */
class CreateApiKeyResponse extends  AbstractModel {
    constructor(){
        super();

        /**
         * The unique request ID, generated by the server, will be returned for every request (if the request fails to reach the server for other reasons, the request will not obtain a RequestId). RequestId is required for locating a problem.
         * @type {string || null}
         */
        this.RequestId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.RequestId = 'RequestId' in params ? params.RequestId : null;

    }
}

/**
 * Main limit package info
 * @class
 */
class TokenPlanPackageInfo extends  AbstractModel {
    constructor(){
        super();

        /**
         * Total quota. The unit is determined by the package type: credits for the Enterprise Professional package and tokens for the Enterprise auto package.
         * @type {string || null}
         */
        this.TotalQuota = null;

        /**
         * Total used quota. The unit varies by package type: credits (enterprise edition professional package), tokens (enterprise edition auto package)
         * @type {string || null}
         */
        this.TotalUsed = null;

        /**
         * Total number of periods.
         * @type {number || null}
         */
        this.TotalCycles = null;

        /**
         * Period unit. Value: month
         * @type {string || null}
         */
        this.CycleUnit = null;

        /**
         * Package effective time.
         * @type {string || null}
         */
        this.StartTime = null;

        /**
         * Package expiration time.
         * @type {string || null}
         */
        this.ExpireTime = null;

        /**
         * Allocated quota for dedicated pool. The unit varies by package type: credits (enterprise edition professional package), tokens (enterprise edition auto package)
         * @type {string || null}
         */
        this.ExclusiveAllocated = null;

        /**
         * Used credit of the dedicated pool. The unit varies based on the package type: credits for the enterprise professional package, and tokens for the enterprise auto package.
         * @type {string || null}
         */
        this.ExclusiveUsed = null;

        /**
         * Total shared pool quota. The measurement unit varies based on the package type: credits (enterprise edition professional package), tokens (enterprise edition auto package).
         * @type {string || null}
         */
        this.SharedPool = null;

        /**
         * Shared used credit. The unit varies by package type: credits (enterprise edition professional package), tokens (enterprise edition auto package)
         * @type {string || null}
         */
        this.SharedUsed = null;

        /**
         * Current period limit. The unit varies by package type: credits (Enterprise Edition Professional), tokens (Enterprise Edition auto).
         * @type {string || null}
         */
        this.CycleQuota = null;

        /**
         * Current cycle.
         * @type {number || null}
         */
        this.CurrentCycle = null;

        /**
         * Remaining cycle.
         * @type {number || null}
         */
        this.RemainCycles = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.TotalQuota = 'TotalQuota' in params ? params.TotalQuota : null;
        this.TotalUsed = 'TotalUsed' in params ? params.TotalUsed : null;
        this.TotalCycles = 'TotalCycles' in params ? params.TotalCycles : null;
        this.CycleUnit = 'CycleUnit' in params ? params.CycleUnit : null;
        this.StartTime = 'StartTime' in params ? params.StartTime : null;
        this.ExpireTime = 'ExpireTime' in params ? params.ExpireTime : null;
        this.ExclusiveAllocated = 'ExclusiveAllocated' in params ? params.ExclusiveAllocated : null;
        this.ExclusiveUsed = 'ExclusiveUsed' in params ? params.ExclusiveUsed : null;
        this.SharedPool = 'SharedPool' in params ? params.SharedPool : null;
        this.SharedUsed = 'SharedUsed' in params ? params.SharedUsed : null;
        this.CycleQuota = 'CycleQuota' in params ? params.CycleQuota : null;
        this.CurrentCycle = 'CurrentCycle' in params ? params.CurrentCycle : null;
        this.RemainCycles = 'RemainCycles' in params ? params.RemainCycles : null;

    }
}

/**
 * CreateTokenPlanTeamOrderAndBuy response structure.
 * @class
 */
class CreateTokenPlanTeamOrderAndBuyResponse extends  AbstractModel {
    constructor(){
        super();

        /**
         * <p>Tencent Cloud order ID. Used to associate all sub-orders under a purchase operation.</p>
         * @type {string || null}
         */
        this.BigOrderId = null;

        /**
         * The unique request ID, generated by the server, will be returned for every request (if the request fails to reach the server for other reasons, the request will not obtain a RequestId). RequestId is required for locating a problem.
         * @type {string || null}
         */
        this.RequestId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.BigOrderId = 'BigOrderId' in params ? params.BigOrderId : null;
        this.RequestId = 'RequestId' in params ? params.RequestId : null;

    }
}

/**
 * DeleteApiKey request structure.
 * @class
 */
class DeleteApiKeyRequest extends  AbstractModel {
    constructor(){
        super();

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }

    }
}

/**
 * Filter criteria
 * @class
 */
class RequestFilter extends  AbstractModel {
    constructor(){
        super();

        /**
         * Filter field name.
         * @type {string || null}
         */
        this.Name = null;

        /**
         * Filter operator. Values: EXACT (exact match), FUZZY (fuzzy matching), NOT (exclusion).
         * @type {string || null}
         */
        this.Op = null;

        /**
         * Filter value list. Supports up to 10.
         * @type {Array.<string> || null}
         */
        this.Values = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.Name = 'Name' in params ? params.Name : null;
        this.Op = 'Op' in params ? params.Op : null;
        this.Values = 'Values' in params ? params.Values : null;

    }
}

/**
 * Main package Token summary
 * @class
 */
class TokenSummary extends  AbstractModel {
    constructor(){
        super();

        /**
         * Package serial number of the current billing cycle
         * @type {number || null}
         */
        this.CycleSeq = null;

        /**
         * Package billing cycle start time (RFC3339)
         * @type {string || null}
         */
        this.CycleStartTime = null;

        /**
         * Package billing cycle end time (RFC3339)
         * @type {string || null}
         */
        this.CycleEndTime = null;

        /**
         * Summary list of tokens grouped by billing item
         * @type {Array.<TokenSummaryBillingItem> || null}
         */
        this.BillingItems = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.CycleSeq = 'CycleSeq' in params ? params.CycleSeq : null;
        this.CycleStartTime = 'CycleStartTime' in params ? params.CycleStartTime : null;
        this.CycleEndTime = 'CycleEndTime' in params ? params.CycleEndTime : null;

        if (params.BillingItems) {
            this.BillingItems = new Array();
            for (let z in params.BillingItems) {
                let obj = new TokenSummaryBillingItem();
                obj.deserialize(params.BillingItems[z]);
                this.BillingItems.push(obj);
            }
        }

    }
}

/**
 * DescribeTokenPlanList request structure.
 * @class
 */
class DescribeTokenPlanListRequest extends  AbstractModel {
    constructor(){
        super();

        /**
         * Offset of paginated query. Default value: 0.
         * @type {number || null}
         */
        this.Offset = null;

        /**
         * Number of results returned by paging query. Default value: 20. Maximum value: 100.
         * @type {number || null}
         */
        this.Limit = null;

        /**
         * Paginate the query filter criteria list. Supported filter fields: TeamId (Package ID), Name (package name), StopReason (disable reason), ProductType (package type).
         * @type {Array.<RequestFilter> || null}
         */
        this.Filters = null;

        /**
         * List of sorting criteria. Supported sorting fields: CreatedAt (creation time) and UpdatedAt (update time). By default, results are sorted by CreatedAt in descending order.
         * @type {Array.<RequestSort> || null}
         */
        this.Sorts = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.Offset = 'Offset' in params ? params.Offset : null;
        this.Limit = 'Limit' in params ? params.Limit : null;

        if (params.Filters) {
            this.Filters = new Array();
            for (let z in params.Filters) {
                let obj = new RequestFilter();
                obj.deserialize(params.Filters[z]);
                this.Filters.push(obj);
            }
        }

        if (params.Sorts) {
            this.Sorts = new Array();
            for (let z in params.Sorts) {
                let obj = new RequestSort();
                obj.deserialize(params.Sorts[z]);
                this.Sorts.push(obj);
            }
        }

    }
}

/**
 * UpgradeTokenPlanTeamOrder response structure.
 * @class
 */
class UpgradeTokenPlanTeamOrderResponse extends  AbstractModel {
    constructor(){
        super();

        /**
         * Tencent Cloud order ID. Used to associate all sub-orders under an upgrade operation.
         * @type {string || null}
         */
        this.BigOrderId = null;

        /**
         * The unique request ID, generated by the server, will be returned for every request (if the request fails to reach the server for other reasons, the request will not obtain a RequestId). RequestId is required for locating a problem.
         * @type {string || null}
         */
        this.RequestId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.BigOrderId = 'BigOrderId' in params ? params.BigOrderId : null;
        this.RequestId = 'RequestId' in params ? params.RequestId : null;

    }
}

/**
 * ModifyGlossaryEntries response structure.
 * @class
 */
class ModifyGlossaryEntriesResponse extends  AbstractModel {
    constructor(){
        super();

        /**
         * Terminology entry list after modification.
         * @type {Array.<GlossaryEntryItem> || null}
         */
        this.Entries = null;

        /**
         * The unique request ID, generated by the server, will be returned for every request (if the request fails to reach the server for other reasons, the request will not obtain a RequestId). RequestId is required for locating a problem.
         * @type {string || null}
         */
        this.RequestId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }

        if (params.Entries) {
            this.Entries = new Array();
            for (let z in params.Entries) {
                let obj = new GlossaryEntryItem();
                obj.deserialize(params.Entries[z]);
                this.Entries.push(obj);
            }
        }
        this.RequestId = 'RequestId' in params ? params.RequestId : null;

    }
}

/**
 * DescribeTokenPlan response structure.
 * @class
 */
class DescribeTokenPlanResponse extends  AbstractModel {
    constructor(){
        super();

        /**
         * Package ID
         * @type {string || null}
         */
        this.TeamId = null;

        /**
         * Package name.
         * @type {string || null}
         */
        this.Name = null;

        /**
         * Root account APP ID.
         * @type {string || null}
         */
        this.AppId = null;

        /**
         * Main account UIN.
         * @type {string || null}
         */
        this.Uin = null;

        /**
         * Status. Valid values: enable, disable.
         * @type {string || null}
         */
        this.Status = null;

        /**
         * Disablement reason. Value: NORMAL, ISOLATED, FROZEN, EXHAUSTED, DESTROYED.
         * @type {string || null}
         */
        this.StopReason = null;

        /**
         * Maximum number of API Keys that can be created.
         * @type {number || null}
         */
        this.ApiKeyMax = null;

        /**
         * Cloud billing prepaid resource package ID.
         * @type {string || null}
         */
        this.PrepayResourceID = null;

        /**
         * Creator. Packages created by a sub-account show the sub-account UIN.
         * @type {string || null}
         */
        this.Creator = null;

        /**
         * Creation time.
         * @type {string || null}
         */
        this.CreatedAt = null;

        /**
         * Update time.
         * @type {string || null}
         */
        this.UpdatedAt = null;

        /**
         * Basic information of the package.
         * @type {TokenPlanPackageInfo || null}
         */
        this.PackageInfo = null;

        /**
         * Auto-renewal flag. Value: 0 (manual renewal), 1 (auto renewal), 2 (no automatic renewal). It is not returned if not bound to a prepaid resource.
         * @type {number || null}
         */
        this.AutoRenewFlag = null;

        /**
         * Current number of created API Keys.
         * @type {number || null}
         */
        this.ApiKeyCount = null;

        /**
         * Token usage details in the current cycle
         * @type {TokenSummary || null}
         */
        this.TokenSummary = null;

        /**
         * Package type. Values: enterprise (Enterprise Professional package), enterprise-auto (Enterprise Light package)
         * @type {string || null}
         */
        this.ProductType = null;

        /**
         * The unique request ID, generated by the server, will be returned for every request (if the request fails to reach the server for other reasons, the request will not obtain a RequestId). RequestId is required for locating a problem.
         * @type {string || null}
         */
        this.RequestId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.TeamId = 'TeamId' in params ? params.TeamId : null;
        this.Name = 'Name' in params ? params.Name : null;
        this.AppId = 'AppId' in params ? params.AppId : null;
        this.Uin = 'Uin' in params ? params.Uin : null;
        this.Status = 'Status' in params ? params.Status : null;
        this.StopReason = 'StopReason' in params ? params.StopReason : null;
        this.ApiKeyMax = 'ApiKeyMax' in params ? params.ApiKeyMax : null;
        this.PrepayResourceID = 'PrepayResourceID' in params ? params.PrepayResourceID : null;
        this.Creator = 'Creator' in params ? params.Creator : null;
        this.CreatedAt = 'CreatedAt' in params ? params.CreatedAt : null;
        this.UpdatedAt = 'UpdatedAt' in params ? params.UpdatedAt : null;

        if (params.PackageInfo) {
            let obj = new TokenPlanPackageInfo();
            obj.deserialize(params.PackageInfo)
            this.PackageInfo = obj;
        }
        this.AutoRenewFlag = 'AutoRenewFlag' in params ? params.AutoRenewFlag : null;
        this.ApiKeyCount = 'ApiKeyCount' in params ? params.ApiKeyCount : null;

        if (params.TokenSummary) {
            let obj = new TokenSummary();
            obj.deserialize(params.TokenSummary)
            this.TokenSummary = obj;
        }
        this.ProductType = 'ProductType' in params ? params.ProductType : null;
        this.RequestId = 'RequestId' in params ? params.RequestId : null;

    }
}

/**
 * CreateTokenPlanApiKeys request structure.
 * @class
 */
class CreateTokenPlanApiKeysRequest extends  AbstractModel {
    constructor(){
        super();

        /**
         * Package ID. You can obtain it through the DescribeTokenPlanList API.
         * @type {string || null}
         */
        this.TeamId = null;

        /**
         * API key name, up to 128 characters. If the number of API keys created exceeds 1, the actual name format is {ApiKeyName}-{serial number} (for example, mykey-1, mykey-2).
         * @type {string || null}
         */
        this.ApiKeyName = null;

        /**
         * Number of creations. Value range: 1–10.
         * @type {number || null}
         */
        this.Count = null;

        /**
         * List of available models. If the package type is the enterprise edition professional package, you can specify a model or pass in "all". "all" means all models supported by the package are available for use. To specify specific models, pass in Model IDs. "all" and specific Model IDs cannot be specified at the same time. If not provided, it indicates the API Key does not support any models, thereby impacting normal use of the API Key. If the package type is the enterprise edition lite package, this field will be force overwritten to ["auto"] regardless of whether it is provided and what value is passed in.
         * @type {Array.<string> || null}
         */
        this.AllowedModels = null;

        /**
         * Exclusive reserved quota. If not passed in, the value is `0`, which means no exclusive reserved quota is assigned to the API Key. Measurement units are as follows:
-Package type is professional, unit value is points;
-Package type is lite package, and the measurement unit is token.
         * @type {number || null}
         */
        this.ExclusiveQuota = null;

        /**
         * Total credit limit. -1 means unlimited. It must be -1 or greater than or equal to the current ExclusiveQuota of the API Key. If not passed, no upper limit is set. The units are as follows:
-Package type is professional, unit value is points;
-Package type is lite package, and the measurement unit is token.
         * @type {number || null}
         */
        this.TotalQuota = null;

        /**
         * TPM (Tokens Per Minute) limit. If not passed, the plan-level TPM is used. Must be >= 0 and <= the package TPM.
         * @type {number || null}
         */
        this.TPM = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.TeamId = 'TeamId' in params ? params.TeamId : null;
        this.ApiKeyName = 'ApiKeyName' in params ? params.ApiKeyName : null;
        this.Count = 'Count' in params ? params.Count : null;
        this.AllowedModels = 'AllowedModels' in params ? params.AllowedModels : null;
        this.ExclusiveQuota = 'ExclusiveQuota' in params ? params.ExclusiveQuota : null;
        this.TotalQuota = 'TotalQuota' in params ? params.TotalQuota : null;
        this.TPM = 'TPM' in params ? params.TPM : null;

    }
}

/**
 * DeleteTokenPlanApiKey request structure.
 * @class
 */
class DeleteTokenPlanApiKeyRequest extends  AbstractModel {
    constructor(){
        super();

        /**
         * API Key ID. You can obtain it through the DescribeTokenPlanApiKeyList API.
         * @type {string || null}
         */
        this.ApiKeyId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.ApiKeyId = 'ApiKeyId' in params ? params.ApiKeyId : null;

    }
}

/**
 * DescribeApiKey response structure.
 * @class
 */
class DescribeApiKeyResponse extends  AbstractModel {
    constructor(){
        super();

        /**
         * API Key ID.
         * @type {string || null}
         */
        this.ApiKeyId = null;

        /**
         * Name
         * @type {string || null}
         */
        this.Name = null;

        /**
         * API Key value (plaintext).
         * @type {string || null}
         */
        this.ApiKey = null;

        /**
         * Remarks.
         * @type {string || null}
         */
        this.Remark = null;

        /**
         * Platform type. Enumerate: maas.
         * @type {string || null}
         */
        this.Platform = null;

        /**
         * Root account.
         * @type {string || null}
         */
        this.Uin = null;

        /**
         * Sub-account.
         * @type {string || null}
         */
        this.SubUin = null;

        /**
         * Status. Valid values: enable, disable.
         * @type {string || null}
         */
        this.Status = null;

        /**
         * Binding type. Value: all (all models and access points), model_all_endpoint_custom (all models + custom access point), model_custom_endpoint_all (custom model + all access points), model_custom_endpoint_custom (custom model + custom access point).
         * @type {string || null}
         */
        this.BindType = null;

        /**
         * Creation time. Format: YYYY-MM-DD HH:mm:ss.
         * @type {string || null}
         */
        this.CreateTime = null;

        /**
         * Last update time. Format: YYYY-MM-DD HH:mm:ss.
         * @type {string || null}
         */
        this.UpdateTime = null;

        /**
         * App ID.
         * @type {string || null}
         */
        this.AppId = null;

        /**
         * Whether it is editable. true means editable, false means non-editable.
         * @type {boolean || null}
         */
        this.Editable = null;

        /**
         * List of bound resources, case-sensitive for endpoint and model kind.
         * @type {Array.<BindingItem> || null}
         */
        this.BindingItems = null;

        /**
         * IP allowlist. Supports IPv4 and CIDR format. Empty array indicates no restriction.
         * @type {Array.<string> || null}
         */
        this.IpWhitelist = null;

        /**
         * This field is empty when Platform is maas.
         * @type {string || null}
         */
        this.Creator = null;

        /**
         * Multi-dimensional information of Token quota. This field is not returned when unconfigured.
         * @type {Array.<QuotaInfo> || null}
         */
        this.QuotaSet = null;

        /**
         * Token quota status. An empty string means no configuration. active means configured with current availability. inactive means configured but quota exhausted.
         * @type {string || null}
         */
        this.QuotaStatus = null;

        /**
         * The unique request ID, generated by the server, will be returned for every request (if the request fails to reach the server for other reasons, the request will not obtain a RequestId). RequestId is required for locating a problem.
         * @type {string || null}
         */
        this.RequestId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.ApiKeyId = 'ApiKeyId' in params ? params.ApiKeyId : null;
        this.Name = 'Name' in params ? params.Name : null;
        this.ApiKey = 'ApiKey' in params ? params.ApiKey : null;
        this.Remark = 'Remark' in params ? params.Remark : null;
        this.Platform = 'Platform' in params ? params.Platform : null;
        this.Uin = 'Uin' in params ? params.Uin : null;
        this.SubUin = 'SubUin' in params ? params.SubUin : null;
        this.Status = 'Status' in params ? params.Status : null;
        this.BindType = 'BindType' in params ? params.BindType : null;
        this.CreateTime = 'CreateTime' in params ? params.CreateTime : null;
        this.UpdateTime = 'UpdateTime' in params ? params.UpdateTime : null;
        this.AppId = 'AppId' in params ? params.AppId : null;
        this.Editable = 'Editable' in params ? params.Editable : null;

        if (params.BindingItems) {
            this.BindingItems = new Array();
            for (let z in params.BindingItems) {
                let obj = new BindingItem();
                obj.deserialize(params.BindingItems[z]);
                this.BindingItems.push(obj);
            }
        }
        this.IpWhitelist = 'IpWhitelist' in params ? params.IpWhitelist : null;
        this.Creator = 'Creator' in params ? params.Creator : null;

        if (params.QuotaSet) {
            this.QuotaSet = new Array();
            for (let z in params.QuotaSet) {
                let obj = new QuotaInfo();
                obj.deserialize(params.QuotaSet[z]);
                this.QuotaSet.push(obj);
            }
        }
        this.QuotaStatus = 'QuotaStatus' in params ? params.QuotaStatus : null;
        this.RequestId = 'RequestId' in params ? params.RequestId : null;

    }
}

/**
 * DescribeTokenPlanApiKeyList request structure.
 * @class
 */
class DescribeTokenPlanApiKeyListRequest extends  AbstractModel {
    constructor(){
        super();

        /**
         * Package ID. You can obtain it through the DescribeTokenPlanList API.
         * @type {string || null}
         */
        this.TeamId = null;

        /**
         * Offset of paginated query. Default value: 0.
         * @type {number || null}
         */
        this.Offset = null;

        /**
         * Number of results returned by paging query. Default value: 20. Maximum value: 100.
         * @type {number || null}
         */
        this.Limit = null;

        /**
         * Paginate the list of query filter criteria. Supported filter fields: ApiKeyId (API Key ID), Name (API Key name), Status (whether the API Key is available), StopReason (reason for disabling the API Key), UseStatus (API Key user-side switch).
         * @type {Array.<RequestFilter> || null}
         */
        this.Filters = null;

        /**
         * Paginate the list of sorting criteria. Supported sorting fields: CreatedAt (creation time) and UpdatedAt (update time). By default, results are sorted by CreatedAt in descending order.
         * @type {Array.<RequestSort> || null}
         */
        this.Sorts = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.TeamId = 'TeamId' in params ? params.TeamId : null;
        this.Offset = 'Offset' in params ? params.Offset : null;
        this.Limit = 'Limit' in params ? params.Limit : null;

        if (params.Filters) {
            this.Filters = new Array();
            for (let z in params.Filters) {
                let obj = new RequestFilter();
                obj.deserialize(params.Filters[z]);
                this.Filters.push(obj);
            }
        }

        if (params.Sorts) {
            this.Sorts = new Array();
            for (let z in params.Sorts) {
                let obj = new RequestSort();
                obj.deserialize(params.Sorts[z]);
                this.Sorts.push(obj);
            }
        }

    }
}

/**
 * DescribeTokenPlanApiKeySecret request structure.
 * @class
 */
class DescribeTokenPlanApiKeySecretRequest extends  AbstractModel {
    constructor(){
        super();

        /**
         * API Key ID. You can obtain it through the DescribeTokenPlanApiKeyList API.
         * @type {string || null}
         */
        this.ApiKeyId = null;

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }
        this.ApiKeyId = 'ApiKeyId' in params ? params.ApiKeyId : null;

    }
}

/**
 * CreateApiKey request structure.
 * @class
 */
class CreateApiKeyRequest extends  AbstractModel {
    constructor(){
        super();

    }

    /**
     * @private
     */
    deserialize(params) {
        if (!params) {
            return;
        }

    }
}

module.exports = {
    DescribeTokenPlanListResponse: DescribeTokenPlanListResponse,
    GlossaryItem: GlossaryItem,
    BindingItem: BindingItem,
    DescribeTokenPlanApiKeySecretResponse: DescribeTokenPlanApiKeySecretResponse,
    TokenPlanApiKeyListItem: TokenPlanApiKeyListItem,
    CreateApiKeysResultItem: CreateApiKeysResultItem,
    GlossaryEntryInput: GlossaryEntryInput,
    ModifyTokenPlanApiKeySecretResponse: ModifyTokenPlanApiKeySecretResponse,
    DescribeTokenPlanApiKeyListResponse: DescribeTokenPlanApiKeyListResponse,
    DeleteGlossaryEntryInput: DeleteGlossaryEntryInput,
    DescribeTokenPlanRequest: DescribeTokenPlanRequest,
    GlossaryEntryItem: GlossaryEntryItem,
    DeleteGlossaryResponse: DeleteGlossaryResponse,
    DescribeTokenPlanApiKeyResponse: DescribeTokenPlanApiKeyResponse,
    ModifyTokenPlanApiKeySecretRequest: ModifyTokenPlanApiKeySecretRequest,
    SubPackageBalance: SubPackageBalance,
    DeleteGlossaryEntriesResponse: DeleteGlossaryEntriesResponse,
    DescribeApiKeyListResponse: DescribeApiKeyListResponse,
    RenewTokenPlanTeamOrderResponse: RenewTokenPlanTeamOrderResponse,
    TokenPlanApiKeyInfo: TokenPlanApiKeyInfo,
    ModifyApiKeyInfoResponse: ModifyApiKeyInfoResponse,
    ModifyTokenPlanApiKeyResponse: ModifyTokenPlanApiKeyResponse,
    DescribeApiKeyListRequest: DescribeApiKeyListRequest,
    CreateGlossaryRequest: CreateGlossaryRequest,
    DescribeGlossaryEntriesResponse: DescribeGlossaryEntriesResponse,
    DeleteGlossaryRequest: DeleteGlossaryRequest,
    DescribeGlossaryEntriesRequest: DescribeGlossaryEntriesRequest,
    DeleteGlossaryEntriesRequest: DeleteGlossaryEntriesRequest,
    CreateGlossaryEntriesResponse: CreateGlossaryEntriesResponse,
    CreateTokenPlanApiKeysResponse: CreateTokenPlanApiKeysResponse,
    DescribeTokenPlanApiKeyRequest: DescribeTokenPlanApiKeyRequest,
    DescribeApiKeyRequest: DescribeApiKeyRequest,
    DeleteApiKeyResponse: DeleteApiKeyResponse,
    ModifyGlossaryEntryInput: ModifyGlossaryEntryInput,
    ModifyTokenPlanApiKeyRequest: ModifyTokenPlanApiKeyRequest,
    TokenSummaryBillingItem: TokenSummaryBillingItem,
    DescribeTokenPlanApiKeyUsageDetailResponse: DescribeTokenPlanApiKeyUsageDetailResponse,
    RequestSort: RequestSort,
    ModifyApiKeyInfoRequest: ModifyApiKeyInfoRequest,
    TokenPlanListItem: TokenPlanListItem,
    CreateGlossaryResponse: CreateGlossaryResponse,
    CreateTokenPlanTeamOrderAndBuyRequest: CreateTokenPlanTeamOrderAndBuyRequest,
    ModifyGlossaryEntriesRequest: ModifyGlossaryEntriesRequest,
    RenewTokenPlanTeamOrderRequest: RenewTokenPlanTeamOrderRequest,
    DescribeGlossariesRequest: DescribeGlossariesRequest,
    DeleteTokenPlanApiKeyResponse: DeleteTokenPlanApiKeyResponse,
    DescribeTokenPlanApiKeyUsageDetailRequest: DescribeTokenPlanApiKeyUsageDetailRequest,
    UpgradeTokenPlanTeamOrderRequest: UpgradeTokenPlanTeamOrderRequest,
    ModifyApiKeyStatusRequest: ModifyApiKeyStatusRequest,
    QuotaInfo: QuotaInfo,
    CreateGlossaryEntriesRequest: CreateGlossaryEntriesRequest,
    ApiKeyDetail: ApiKeyDetail,
    DescribeGlossariesResponse: DescribeGlossariesResponse,
    ModifyApiKeyStatusResponse: ModifyApiKeyStatusResponse,
    BatchCreateFailedItem: BatchCreateFailedItem,
    CreateApiKeyResponse: CreateApiKeyResponse,
    TokenPlanPackageInfo: TokenPlanPackageInfo,
    CreateTokenPlanTeamOrderAndBuyResponse: CreateTokenPlanTeamOrderAndBuyResponse,
    DeleteApiKeyRequest: DeleteApiKeyRequest,
    RequestFilter: RequestFilter,
    TokenSummary: TokenSummary,
    DescribeTokenPlanListRequest: DescribeTokenPlanListRequest,
    UpgradeTokenPlanTeamOrderResponse: UpgradeTokenPlanTeamOrderResponse,
    ModifyGlossaryEntriesResponse: ModifyGlossaryEntriesResponse,
    DescribeTokenPlanResponse: DescribeTokenPlanResponse,
    CreateTokenPlanApiKeysRequest: CreateTokenPlanApiKeysRequest,
    DeleteTokenPlanApiKeyRequest: DeleteTokenPlanApiKeyRequest,
    DescribeApiKeyResponse: DescribeApiKeyResponse,
    DescribeTokenPlanApiKeyListRequest: DescribeTokenPlanApiKeyListRequest,
    DescribeTokenPlanApiKeySecretRequest: DescribeTokenPlanApiKeySecretRequest,
    CreateApiKeyRequest: CreateApiKeyRequest,

}
